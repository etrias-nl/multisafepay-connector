name: Quality Assurance

'on':
    push:
        branches: [ master ]
    pull_request:
        branches: [ master ]

jobs:
    checks:
        runs-on: ubuntu-latest
        steps:
            -
                uses: actions/checkout@v3.6.0
            -
                uses: ScribeMD/docker-cache@0.3.4
                with:
                    key: ${{ runner.os }}-docker-${{ hashFiles('Makefile', 'composer.json') }}
            -
                run: make lint
